import 'package:flutter/material.dart';

class EndBrandItem extends StatelessWidget {
  const EndBrandItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        const Text('Congratulations!'),
      ],
    );
  }
}
